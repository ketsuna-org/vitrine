backend:
  name: github
  repo: ketsuna-org/vitrine
  branch: master
  use_graphql: true
  base_url: https://decap-proxy.ketsuna.workers.dev
  squash_merges: true

site_url: https://bot-creator.fr
publish_mode: editorial_workflow
media_folder: assets
public_folder: assets

collections:
  - name: tutorials_en
    label: Tutorials
    label_singular: Tutorial
    folder: _posts
    create: true
    filter:
      field: locale
      value: en
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    summary: "{{title}}"
    fields:
      - { label: Title, name: title, widget: string }
      - { label: Description, name: description, widget: string }
      - {
          label: Primary Category,
          name: category,
          widget: select,
          options: ["Getting Started", "Building Commands", "Advanced Topics", "Reference & Deployment"],
          default: "Getting Started",
          required: true
        }
      - { label: Function Syntax, name: function_syntax, widget: string, required: false, hint: "For BDFD functions, e.g., $ping or $customEmbed[title;text]" }
      - { label: Date, name: date, widget: datetime, required: false }
      - {
          label: Categories,
          name: categories,
          widget: list,
          required: false,
          allow_add: true,
        }
      - { label: Featured Image, name: thumbnail, widget: image, required: false }
      - { label: Author, name: author, widget: string, default: Garder500 }
      - {
          label: Translation Key,
          name: translation_key,
          widget: string,
          hint: Unique key for related content (optional),
        }
      - { label: Locale, name: locale, widget: hidden, default: en }
      - { label: Content Language, name: content_language, widget: hidden, default: en }
      - { label: Layout, name: layout, widget: hidden, default: post }
      - { label: Generate TOC, name: toc, widget: boolean, default: true }
      - { label: Body, name: body, widget: markdown }
